using System;
using UnityEngine;

public class #SCRIPTNAME# : BaseState
{
    private readonly YourAIType _owner;
    
    public #SCRIPTNAME#(YourAIType owner) : base(owner.gameObject) {
        _owner = owner;
    }

    // Runs every frame
    public override Type Tick() {
        return null;
    }
    
    // Runs when we enter this state
    public override void OnEnter(BaseState oldState){
    
    }
    
    // Runs when we exit this state
    public override void OnExit(BaseState newState) {
    
    }
}